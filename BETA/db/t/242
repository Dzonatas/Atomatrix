o6 p1 c383 s75 l254 c430 w0 p0 v2
name	(6,91)	$ process
spawnd	(6,91)	# 2
.
fork (0)
p = $proc.fork();
p @ $error ? p!
p ? {
	this.id = p;
	this!
	}
!
/* $proc.exit() */

wait (0)
$proc.wait() ? 1!

